# Copy to .env (gitignored). All values are secrets or local settings.
POSTGRES_PASSWORD=changeme-postgres
ACTUAL_PASSWORD=changeme-actual
ACTUAL_FILE=Household
# ACTUAL_ENCRYPTION_PASSWORD=            # only when the budget uses end-to-end encryption
PIPELINE_API_TOKEN=changeme-long-random-token
LOG_LEVEL=INFO
# N8N_NETWORK=n8n_default                # docker network of your running n8n
